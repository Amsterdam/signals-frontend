// import { fromJS } from 'immutable';
// import { selectMapContainerDomain } from 'selectors';

describe('selectMapContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
