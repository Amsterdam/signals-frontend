{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "$ref": "#/definitions/Config",
  "definitions": {
    "Config": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "head": {
          "$ref": "#/definitions/Head"
        },
        "logo": {
          "$ref": "#/definitions/Logo"
        },
        "sentry": {
          "$ref": "#/definitions/Sentry"
        },
        "matomo": {
          "$ref": "#/definitions/Matomo"
        },
        "language": {
          "$ref": "#/definitions/Language"
        },
        "links": {
          "$ref": "#/definitions/Links"
        },
        "theme": {
          "$ref": "#/definitions/Theme"
        },
        "map": {
          "$ref": "#/definitions/Map"
        },
        "areaTypeCodeForDistrict": {
          "description": "The area_type_code that is being used as districts, fetched from the areas endpoint (when [fetchDistrictsFromBackend](#fetchDistrictsFromBackend) is set to true).",
          "type": "string"
        },
        "fetchDistrictsFromBackend": {
          "description": "Feature switch to use the areas endpoint to fetch districts, instead of using the hardcoded stadsdelen",
          "type": "boolean"
        },
        "fetchQuestionsFromBackend": {
          "description": " When true, will bypass getting the configuration for the incident submission form questions from local modules. Instead, a request to an API endpoint is made to retrieve the configuration. See [QUESTIONS_ENDPOINT](#QUESTIONS_ENDPOINT)",
          "type": "boolean"
        },
        "showVulaanControls": {
          "description": "When false, will not show extra questions after the first page of questions in the incident submission form",
          "type": "boolean"
        },
        "AUTH_ME_ENDPOINT": {
          "description": "URL for the authorization endpoint; retrieves user profile data\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_user_auth_me for reference",
          "type": "string",
          "format": "uri"
        },
        "AREAS_ENDPOINT": {
          "description": "URL for the areas endpoint; retrieves geographical areas\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_private_areas_geography_ for reference",
          "type": "string",
          "format": "uri"
        },
        "CATEGORIES_ENDPOINT": {
          "description": "URL for the public categories endpoint. Used for retrieving subcategory and category data when posting to the [INCIDENT_PUBLIC_ENDPOINT](#INCIDENT_PUBLIC_ENDPOINT) and [INCIDENT_PRIVATE_ENDPOINT](#INCIDENT_PRIVATE_ENDPOINT)\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_public_terms_categories for reference",
          "type": "string",
          "format": "uri"
        },
        "CATEGORIES_PRIVATE_ENDPOINT": {
          "description": "URL for the private categories endpoint. Used by the settings module to `GET` and `PATCH` categories \n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_private_categories_ for reference",
          "type": "string",
          "format": "uri"
        },
        "DEPARTMENTS_ENDPOINT": {
          "description": "URL for the private departments endpoint. Used by the settings module to `GET` and `PATCH` departments\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_private_departments_ for reference",
          "type": "string",
          "format": "uri"
        },
        "FEEDBACK_FORMS_ENDPOINT": {
          "description": "URL for the public feedback endpoint. Used by the KTO (klanttevredenheidsonderzoek) module to verify the validity of a request\n\nNo Swagger documentation available.",
          "type": "string",
          "format": "uri"
        },
        "FEEDBACK_STANDARD_ANSWERS_ENDPOINT": {
          "description": "URL for the public feedback answers endpoint. Used by the KTO (klanttevredenheidsonderzoek) module to retrieve questions regarding a person's incident handling satisfaction\n\nNo Swagger documentation available.",
          "type": "string",
          "format": "uri"
        },
        "FILTERS_ENDPOINT": {
          "description": "URL for the private filters endpoint. Used to `GET`, `POST` and `PATCH` filters for a specific user\n\nNo Swagger documentation available.",
          "type": "string",
          "format": "uri"
        },
        "GEOGRAPHY_ENDPOINT": {
          "description": "URL for the private incident geolocation endpoint. Used by the incident overview map\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_private_signals_geography for reference",
          "type": "string",
          "format": "uri"
        },
        "IMAGE_ENDPOINT": {
          "description": "URL for the public image endpoint. Used by the incident form to upload images for a specific incident\n\nNo Swagger documentation available",
          "type": "string",
          "format": "uri"
        },
        "INCIDENTS_ENDPOINT": {
          "description": "URL for the private incidents endpoint. Used to GET sets of incidents\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_private_signals_ for reference",
          "type": "string",
          "format": "uri"
        },
        "INCIDENT_PRIVATE_ENDPOINT": {
          "description": "URL for the private incident endpoint. Used to `POST` incident data for authorized users\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/post_signals_v1_private_signals_ for reference",
          "type": "string",
          "format": "uri"
        },
        "INCIDENT_PUBLIC_ENDPOINT": {
          "description": "URL for the public incident endpoint. Used to `POST` incident data for unauthorized users\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/post_signals_v1_public_signals for reference",
          "type": "string",
          "format": "uri"
        },
        "OIDC_AUTH_ENDPOINT": {
          "description": "URL for the oAuth2 service",
          "type": "string",
          "format": "uri"
        },
        "OIDC_CLIENT_ID": {
          "description": "oAuth2 `client_id` service parameter value",
          "type": "string"
        },
        "OIDC_RESPONSE_TYPE": {
          "description": "oAuth2 `response_type` service parameter value",
          "type": "string"
        },
        "OIDC_SCOPE": {
          "description": "oAuth2 `scope` service parameter value (specific to `signals-amsterdam` and `signals-weesp` implementations)",
          "type": "string"
        },
        "OVL_KLOKKEN_LAYER": {
          "description": "GeoJSON URL for the retrieval of public clock location data. Used by the incident form for showing clock locations on a map\n\nNo Swagger documentation available",
          "type": "string",
          "format": "uri"
        },
        "OVL_VERLICHTING_LAYER": {
          "description": "GeoJSON URL for the retrieval of street light data. Used by the incident form for showing street light locations on a map\n\nNo Swagger documentation available",
          "type": "string",
          "format": "uri"
        },
        "PERMISSIONS_ENDPOINT": {
          "description": "URL for the private permissions endpoint. Used by the settings module to `GET` the full set of permissions\n\nNo Swagger documentation available",
          "type": "string",
          "format": "uri"
        },
        "PREDICTION_ENDPOINT": {
          "description": "URL for the public machine learning prediction endpoint. Used by the incident form to get an indication of (sub)category based on user input\n\nNo Swagger documentation available",
          "type": "string",
          "format": "uri"
        },
        "QUESTIONS_ENDPOINT": {
          "description": "URL for the public question endpoint. Used to `GET` question configuration based on the value of the [fetchQuestionsFromBackend](#fetchQuestionsFromBackend) settings prop",
          "type": "string",
          "format": "uri"
        },
        "ROLES_ENDPOINT": {
          "description": "URL for the private roles endpoint. Used by the settings module to `GET`, `PATCH` and `POST` roles\n\nNo Swagger documentation available",
          "type": "string",
          "format": "uri"
        },
        "SEARCH_ENDPOINT": {
          "description": "URL for the private search endpoint. Used by the incident management module to retrieve incident by search query\n\nNo Swagger documentation available",
          "type": "string",
          "format": "uri"
        },
        "STATIC_MAP_SERVER_URL": {
          "description": "URL for the map server that serves static map tile images\n\nUsed in the `MapStatic` (src/components/MapStatic/index.js)",
          "type": "string",
          "format": "uri"
        },
        "TERMS_ENDPOINT": {
          "description": "URL for the private terms endpoint. Used by the incident management module to `GET` and `POST` default response texts for incident handling\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_public_terms_categories for reference",
          "type": "string",
          "format": "uri"
        },
        "USERS_ENDPOINT": {
          "description": "URL for the private users endpoint. Used by the settings module to `GET`, `PATCH` and `POST` users\n\nSee https://api.data.amsterdam.nl/api/swagger/?url=/signals/swagger/openapi.yaml#/default/get_signals_v1_private_users_ for reference",
          "type": "string",
          "format": "uri"
        }
      },
      "required": [
        "AREAS_ENDPOINT",
        "AUTH_ME_ENDPOINT",
        "CATEGORIES_ENDPOINT",
        "CATEGORIES_PRIVATE_ENDPOINT",
        "DEPARTMENTS_ENDPOINT",
        "FEEDBACK_FORMS_ENDPOINT",
        "FEEDBACK_STANDARD_ANSWERS_ENDPOINT",
        "FILTERS_ENDPOINT",
        "GEOGRAPHY_ENDPOINT",
        "IMAGE_ENDPOINT",
        "INCIDENTS_ENDPOINT",
        "INCIDENT_PRIVATE_ENDPOINT",
        "INCIDENT_PUBLIC_ENDPOINT",
        "OIDC_AUTH_ENDPOINT",
        "OIDC_CLIENT_ID",
        "OIDC_RESPONSE_TYPE",
        "OIDC_SCOPE",
        "OVL_KLOKKEN_LAYER",
        "OVL_VERLICHTING_LAYER",
        "PERMISSIONS_ENDPOINT",
        "PREDICTION_ENDPOINT",
        "QUESTIONS_ENDPOINT",
        "ROLES_ENDPOINT",
        "SEARCH_ENDPOINT",
        "STATIC_MAP_SERVER_URL",
        "TERMS_ENDPOINT",
        "USERS_ENDPOINT",
        "fetchDistrictsFromBackend",
        "fetchQuestionsFromBackend",
        "head",
        "language",
        "links",
        "logo",
        "map",
        "matomo",
        "sentry",
        "showVulaanControls",
        "theme"
      ],
      "allOf": [
        {
          "if": {
            "properties": {
              "fetchDistrictsFromBackend": {
                "const": true
              }
            }
          },
          "then": {
            "required": [
              "areaTypeCodeForDistrict"
            ]
          }
        }
      ],
      "title": "Config"
    },
    "Head": {
      "type": "object",
      "additionalProperties": false,
      "description": "Site head configuration",
      "properties": {
        "androidIcon": {
          "type": "string",
          "description": "PNG image in `src/images` folder. Should be an absolute URL since Webpack copies it in the web root on build"
        },
        "backgroundColor": {
          "type": "string",
          "description": "Background color for the splash screen when the application is first launched on mobile"
        },
        "favicon": {
          "description": "PNG image in `src/images` folder",
          "type": "string"
        },
        "iosIcon": {
          "description": "PNG image in `src/images` folder",
          "type": "string"
        },
        "statusBarStyle": {
          "type": "string",
          "description": "Contains value that sets the style of the status bar for the web application. See https://developer.apple.com/library/archive/documentation/AppleApplications/Reference/SafariHTMLRef/Articles/MetaTags.html#:~:text=apple%2Dmobile%2Dweb%2Dapp%2Dstatus%2Dbar%2Dstyle,-Sets%20the%20style&text=If%20set%20to%20black%20%2C%20the,displayed%20below%20the%20status%20bar for reference"
        },
        "themeColor": {
          "type": "string",
          "description": "Value that sets the color of the tool bar, and may be reflected in the app's preview in task switchers"
        }
      },
      "required": [
        "androidIcon",
        "backgroundColor",
        "favicon",
        "iosIcon",
        "statusBarStyle",
        "themeColor"
      ],
      "title": "Head"
    },
    "Language": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "avgDisclaimer": {
          "description": "Text that outlines the use of privacy related information",
          "type": "string"
        },
        "consentToContactSharing": {
          "description": "The message to ask for consent to share contact information",
          "type": "string"
        },
        "district": {
          "description": "The label for the district (stadsdeel/wijk)",
          "type": "string"
        },
        "footer1": {
          "description": "Extra information to show in the footer of the application",
          "type": "string"
        },
        "footer2": {
          "description": "Second line of information to show in the footer of the application",
          "type": "string"
        },
        "headerTitle": {
          "description": "The title to show in the header of the application, next to the logo",
          "type": "string"
        },
        "phoneNumber": {
          "description": "General phone number",
          "type": "string"
        },
        "shortTitle": {
          "description": "PWA `short_name` property value. Should not exceed 12 characters. See https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/manifest.json/short_name for reference",
          "type": "string"
        },
        "siteTitle": {
          "description": "Application title showing up in the title bar and search results",
          "type": "string"
        },
        "smallHeaderTitle": {
          "description": "The title to show in the small version of the header",
          "type": "string"
        },
        "title": {
          "description": "PWA title value. See https://developer.apple.com/library/archive/documentation/AppleApplications/Reference/SafariHTMLRef/Articles/MetaTags.html#:~:text=apple%2Dmobile%2Dweb%2Dapp%2Dstatus%2Dbar%2Dstyle,-Sets%20the%20style&text=If%20set%20to%20black%20%2C%20the,displayed%20below%20the%20status%20bar for reference",
          "type": "string"
        },
        "urgentContactInfo": {
          "description": "Extra information to show in case of an error",
          "type": "string"
        }
      },
      "required": [
        "avgDisclaimer",
        "consentToContactSharing",
        "district",
        "footer1",
        "footer2",
        "headerTitle",
        "phoneNumber",
        "shortTitle",
        "siteTitle",
        "smallHeaderTitle",
        "title",
        "urgentContactInfo"
      ],
      "title": "Language"
    },
    "Links": {
      "description": "Various Links",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "help": {
          "description": "A website with help about the application, for employees of the municipality",
          "type": "string",
          "format": "uri"
        },
        "home": {
          "description": "The home page of the website of the municipality",
          "type": "string",
          "format": "uri"
        },
        "privacy": {
          "description": "The privacy statement of the municipality",
          "type": "string",
          "format": "uri"
        }
      },
      "required": [
        "help",
        "home",
        "privacy"
      ],
      "title": "Links"
    },
    "Logo": {
      "description": "Logo configuration",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "url": {
          "description": "Any URL that will hold the image for the application's logo. Can be a URI or an absolute URL that points to the webroot",
          "type": [
            "string",
            "null"
          ]
        },
        "width": {
          "description": "Length value that sets the logo image's width. See https://developer.mozilla.org/en-US/docs/Web/CSS/width for reference",
          "type": [
            "string",
            "integer"
          ]
        },
        "height": {
          "description": "Length value that sets the logo image's height",
          "type": [
            "string",
            "integer"
          ]
        },
        "smallWidth": {
          "description": "Length value that sets the logo image's width in case the logo is rendered in a site header with limited height",
          "type": [
            "string",
            "integer"
          ]
        },
        "smallHeight": {
          "description": "Length value that sets the logo image's height in case the logo is rendered in a site header with limited height",
          "type": [
            "string",
            "integer"
          ]
        }
      },
      "required": [
        "height",
        "smallHeight",
        "smallWidth",
        "url",
        "width"
      ],
      "title": "Logo"
    },
    "Map": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "municipality": {
          "type": [
            "string",
            "array"
          ]
        },
        "options": {
          "$ref": "#/definitions/MapOptions"
        },
        "optionsBackOffice": {
          "$ref": "#/definitions/OptionsBackOffice"
        },
        "tiles": {
          "$ref": "#/definitions/Tiles"
        }
      },
      "required": [
        "municipality",
        "options",
        "tiles"
      ],
      "title": "Map"
    },
    "MapOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "center": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "maxBounds": {
          "type": "array",
          "items": {
            "type": "array",
            "items": {
              "type": "number"
            }
          }
        },
        "maxZoom": {
          "type": "integer"
        },
        "minZoom": {
          "type": "integer"
        },
        "zoom": {
          "type": "integer"
        }
      },
      "required": [
        "center",
        "maxBounds",
        "maxZoom",
        "minZoom",
        "zoom"
      ],
      "title": "MapOptions"
    },
    "OptionsBackOffice": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "center": {
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "maxZoom": {
          "type": "integer"
        },
        "minZoom": {
          "type": "integer"
        },
        "zoom": {
          "type": "integer"
        }
      },
      "required": [],
      "title": "OptionsBackOffice"
    },
    "Tiles": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "args": {
          "description": "Array of URL template strings to initialise the Leaflet TileLayer. See https://leafletjs.com/reference-1.6.0.html#tilelayer for reference Used in the `Map` (src/components/Map/index.js)",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "options": {
          "description": "Object with Leaflet TileLayer configuration properties. See https://leafletjs.com/reference-1.6.0.html#tilelayer for reference\n\nUsed in the `Map` component (src/components/Map/index.js)",
          "$ref": "#/definitions/TilesOptions"
        }
      },
      "required": [
        "args",
        "options"
      ],
      "title": "Tiles"
    },
    "TilesOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "attribution": {
          "type": "string"
        },
        "subdomains": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "tms": {
          "type": "boolean"
        }
      },
      "required": [
        "attribution",
        "subdomains",
        "tms"
      ],
      "title": "TilesOptions"
    },
    "Matomo": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "urlBase": {
          "type": "string",
          "format": "uri",
          "description": "Matomo tracker base URL. See https://www.npmjs.com/package/@datapunt/matomo-tracker-js for reference"
        },
        "siteId": {
          "description": "ID by which the site is tracked in Matomo",
          "type": "integer"
        }
      },
      "required": [
        "siteId",
        "urlBase"
      ],
      "title": "Matomo"
    },
    "Sentry": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "dsn": {
          "type": "string",
          "format": "uri",
          "description": "Sentry data source name value. When omitted, Sentry will not be initialised in the application"
        }
      },
      "required": [
        "dsn"
      ],
      "title": "Sentry"
    },
    "Theme": {
      "description": "Configuration object that is merged with the application's theme provider configuration and will override any existing theme configuration. See the `amsterdam styled components theme configuration (https://github.com/Amsterdam/amsterdam-styled-components/blob/master/packages/asc-ui/src/theme/default/index.ts) for the structure of the object.\n\nUsed in the `ThemeProvider` component (src/components/ThemeProvider/index.js)",
      "type": "object",
      "additionalProperties": false,
      "title": "Theme"
    }
  }
}
