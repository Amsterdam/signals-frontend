import styled from '@datapunt/asc-core';

const GeocoderStyle = styled.div`
  z-index: 401;
  width: calc(100% - 30px);
  display: flex;
  flex-direction: column;
  position: absolute;
  top: 0;
  margin: 15px;
`;

export default GeocoderStyle;
