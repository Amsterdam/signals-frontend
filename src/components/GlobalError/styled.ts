import styled from 'styled-components'

import { StyledErrorAlert as BaseStyledErrorAlert } from '../ErrorAlert/styled'

export const StyledErrorAlert = styled(BaseStyledErrorAlert)`
  margin-top: 0;
`
