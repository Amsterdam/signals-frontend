import React from 'react';

import Selector from './Selector';

describe('signals/incident/components/form/Selector', () => {
  let props;

  beforeEach(() => {
    props = {
    };
  });

  afterEach(() => {
    jest.resetAllMocks();
  });

  it('should render ', () => {
    expect(true).toBe(true);
  });
});
