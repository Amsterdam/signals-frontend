/*
 * MapContainer Messages
 *
 * This contains all the text for the MapContainer component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.MapContainer.header',
    defaultMessage: 'This is MapContainer container !',
  },
});
